# Tarefas — Fase 1: primeira conferência útil

Escopo exclusivo da Fase 1 do `PLAN.md`. O aceite canônico é: 50 XMLs lidos, 50 processados, 0 ilegíveis, exatamente 2 divergências de total (`NFe_0004.xml` e `NFe_0038.xml`) e nenhum falso positivo da regra de total nas 48 notas sem divergência de total.

Pré-condição: o scaffold de verificação descrito no `AGENTS.md` já existe, foi commitado e passa pelo portão com os testes de aceitação explicitamente pulados. O scaffold não implementa nenhuma tarefa abaixo.

Na coluna **Depende de**, “Nenhuma — independente” indica início livre. Marcações **independente de F1-xx** identificam tarefas que podem avançar em paralelo após suas dependências.

| ID | Tarefa | Arquivo que cria ou altera | Depende de | Critério de pronto |
|---|---|---|---|---|
| F1-01 | Estender o scaffold instalável com a dependência de execução `lxml` e transformar os 50 XMLs no lote canônico. Mover `xmls/` para `tests/fixtures/xmls/`, registrar nomes e hashes e criar o teste de inventário. | `pyproject.toml`; `tests/conftest.py`; `tests/fixtures/xmls/*.xml`; `tests/fixtures/manifest.json`; `tests/test_fixture_inventory.py` | Nenhuma — independente | Em Python 3.11, `python -m pip install -e '.[test]' && python -m pytest tests/test_fixture_inventory.py -q` retorna `0` e mostra `1 passed`; o teste valida `arquivos=50`, `nomes_unicos=50`, `hashes_validos=50`. `pyproject.toml` preserva `requires-python = ">=3.11"`, o marcador `aceitacao` e as dependências de teste/lint, e passa a declarar `lxml` como dependência de execução. Os testes de aceitação continuam pulados, nunca aprovados, porque o sistema ainda não existe. |
| F1-02 | Definir contratos imutáveis de documento, ocorrência, severidade, contagens e contexto de regra. Converter valores monetários da string do XML diretamente para `Decimal` e proibir `float`. | `src/nfe_auditor/domain.py`; `tests/test_domain.py`; `tests/test_decimal_policy.py` | F1-01 — **independente de F1-03** | `python -m pytest tests/test_domain.py tests/test_decimal_policy.py -q` retorna `0`; os testes percorrem os 50 XMLs, validam `vProd` e `vNF` como `Decimal` e uma inspeção da árvore sintática encontra `0` nomes ou chamadas a `float` em `src/` e `tests/`, ignorando comentários, docstrings e literais de texto. |
| F1-03 | Definir CLI e configuração externa com caminhos obrigatórios, tolerância da regra e severidades com nome, bloqueio e rank numérico. Resolver valores somente em tempo de execução. | `src/nfe_auditor/__main__.py`; `src/nfe_auditor/cli.py`; `src/nfe_auditor/config.py`; `config.example.toml`; `tests/test_cli.py`; `tests/test_config.py` | F1-01 — **independente de F1-02** | `python -m pytest tests/test_cli.py tests/test_config.py -q` retorna `0`; `python -m nfe_auditor --help` exibe `--input`, `--output` e `--config`. A configuração carrega tolerância `0.01` como `Decimal('0.01')` e severidade `alta` como bloqueante com rank explícito; tolerância ausente, negativa ou numérica e severidade sem rank produzem retorno `2`. |
| F1-04 | Implementar inventário ordenado, leitura segura e normalização única. Continuar após XML malformado, preservar entradas e fechar as contagens. | `src/nfe_auditor/inventory.py`; `src/nfe_auditor/xml_reader.py`; `tests/test_inventory.py`; `tests/test_xml_reader.py` | F1-02 — **independente de F1-03 e F1-06** | `python -m pytest tests/test_inventory.py tests/test_xml_reader.py -q` retorna `0` e valida no lote original `lidos=50`, `processados=50`, `ilegíveis=0`. Em cópia temporária com um XML corrompido, valida `lidos=50`, `processados=49`, `ilegíveis=1`, uma ocorrência alta no arquivo corrompido e 50 hashes originais inalterados. |
| F1-05 | Criar registry explícito de funções puras e a regra `total_note_vs_products_v1`. Calcular `diferenca = vNF - soma`, alertar por `abs(diferenca) > tolerancia` e preservar o sinal. | `src/nfe_auditor/rules/__init__.py`; `src/nfe_auditor/rules/contracts.py`; `src/nfe_auditor/rules/registry.py`; `src/nfe_auditor/rules/total_note_vs_products_v1.py`; `tests/fixtures/expected_phase1.json`; `tests/test_rule_registry.py`; `tests/test_total_note_vs_products_rule.py` | F1-03 e F1-04 — **independente de F1-06** | `python -m pytest tests/test_rule_registry.py tests/test_total_note_vs_products_rule.py -q` retorna `0`. Com tolerância `0.01`, encontra somente `NFe_0004.xml` e `NFe_0038.xml`, ambas com `-250.00`, e 0 falso positivo. Uma cópia temporária com diferença positiva também é acusada. O registry contém a regra exatamente uma vez e prova uma execução por XML. |
| F1-06 | Gerar `relatorio.csv` determinístico com linhas tipadas, largura uniforme, resumo fixo e ocorrências ordenadas por rank externo, arquivo e regra. | `src/nfe_auditor/report.py`; `tests/test_report.py`; `tests/fixtures/expected_report_phase1.csv` | F1-02 e F1-03 — **independente de F1-04 e F1-05** | `python -m pytest tests/test_report.py -q` retorna `0`. O CSV é byte a byte igual ao esperado: cabeçalho `tipo,arquivo,regra,severidade,motivo,valor`; cinco contadores na ordem `lidos`, `processados`, `ilegiveis`, `bloqueantes`, `avisos`; depois duas ocorrências `alta`, com motivo fixo e `valor=-250.00`. Usa nomes simples, seis colunas em toda linha e 0 dados variáveis; caminhos diferentes geram bytes idênticos. |
| F1-07 | Integrar CLI, configuração, inventário, leitor, registry, regra e relatório num coordenador sem estado. Emitir logs JSON e aplicar retornos `0/1/2`. | `src/nfe_auditor/runner.py`; `src/nfe_auditor/logging.py`; `src/nfe_auditor/cli.py`; `tests/test_runner.py`; `tests/test_exit_codes.py`; `tests/test_structured_logging.py` | F1-03, F1-04, F1-05 e F1-06 | `python -m pytest tests/test_runner.py tests/test_exit_codes.py tests/test_structured_logging.py -q` retorna `0`. A execução dos 50 XMLs registra `lidos=50`, `processados=50`, `ilegíveis=0`, `ocorrências=2`, grava as duas divergências esperadas e retorna `1`. Cenários temporários comprovam retorno `0` sem bloqueante e `2` em falha da execução; toda linha de log é JSON válido. |
| F1-08 | Provar, sem criar ou afrouxar o arnês, que a Fase 1 satisfaz os testes de aceite escritos no scaffold: duas divergências, zero falso positivo, regra genérica, contagens, reprodutibilidade, portabilidade, escrita restrita, execução instalada e desempenho. | Nenhum arquivo novo; valida `tests/test_phase1_acceptance.py`, `verificar.py` e `README.md` criados no scaffold. Correções retornam à tarefa responsável. | F1-07 | `python -m pytest -m aceitacao -v` retorna `0` com `4 passed` e `0 skipped`: encontra as 2 divergências, produz 0 falso positivo, detecta divergência criada numa cópia temporária de nota limpa sem depender do nome e fecha `lidos = processados + ilegíveis`. `python verificar.py` retorna `0`, imprime `total_note_vs_products_v1 esperado=2 achado=2 falso_positivo=0`, `notas_sem_divergencia_de_total=48` e termina com `FASE PROVADA`. Sem `PYTHONPATH`, `python -m nfe_auditor --input tests/fixtures/xmls --output build/output --config config.example.toml` retorna `1` e gera `build/output/relatorio.csv` com cinco linhas de resumo e duas ocorrências. Duas execuções em caminhos diferentes geram CSVs idênticos em menos de 5 minutos e escrevem 0 arquivos indevidos. |

## Tarefas que podem rodar em paralelo

- Após **F1-01**, execute **F1-02** e **F1-03** em paralelo.
- Após **F1-02**, execute **F1-04**; após F1-02 e F1-03, execute **F1-06** em paralelo com F1-04.
- Após **F1-03** e **F1-04**, execute **F1-05** em paralelo com **F1-06**, caso ela ainda esteja em andamento.
- **F1-07** aguarda F1-03 a F1-06. **F1-08** aguarda F1-07.
